<% @movie.each do |x| %>
<b><%= x.title %> </b> (<%=x.year_released %>) - <%= x.description %> <%= link_to "Edit", edit_movie_path(x.id) %> <%= link_to "Delete", x, method: :delete, data: { confirm: 'Are you sure?' } %></br>
<% end %>